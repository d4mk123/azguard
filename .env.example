# Azure service principal credentials for live scans.
# Copy this file to .env and fill in real values (never commit .env).
# Used automatically by DefaultAzureCredential via azure-identity.

AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=
AZURE_SUBSCRIPTION_ID=
